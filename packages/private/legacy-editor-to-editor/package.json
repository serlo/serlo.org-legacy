{
  "name": "@serlo/legacy-editor-to-editor",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "cross-env NODE_ENV=production babel src --out-dir dist --root-mode upward --source-maps",
    "start": "nodemon --legacy-watch --watch src --exec \"yarn build\""
  },
  "dependencies": {
    "@serlo/editor": "^0.5.0",
    "@serlo/markdown": "^0.0.0",
    "uuid": "^3.0.0"
  },
  "private": true
}
